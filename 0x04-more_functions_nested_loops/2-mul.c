#include "main.h"

/**
 * mul - get the multiplication of two numbers
 * @a: this is the first number to be input
 * @b:  this is the second number to be input
 *
 * Return: only the multiplication of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
