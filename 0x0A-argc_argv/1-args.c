#include <stdlib.h>
#include <stdio.h>

/**
 * main -  the main function
 * @argc: Counts arguments passed variable
 * @argv: Argument vector string variable
 *
 * Return: Always 0.
 */

int main(int argc, char *argv[])
{
	argv = argv;

	printf("%d\n", argc - 1);
	return (0);
}
